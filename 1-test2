class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for a in range(i+1,len(nums)):   #range(a,b)----[a,a+1,...b-1]
                if nums[i]+nums[a]==target:
                    return list((i,a)) 
        
